# API 基礎 URL
VITE_API_URL=http://localhost:3001/api

# ============================================================
# VITE_SITE_URL — 網站正式網址（SEO 關鍵設定）
# ============================================================
# 用途：canonical、og:url、JSON-LD 的 url、sitemap.xml 內的所有 <loc>
#       全部以此為根。
#
# ⚠️ 上正式站前必須在 Vercel 專案設定中加入這個「建置環境變數」
#    （Settings → Environment Variables，Environment 勾 Production）。
#    若使用自訂網域（例如 https://coachaaron.com），一定要明確設定，
#    否則會退回 *.vercel.app 網域，導致自訂網域與 vercel.app 網域
#    互相競爭索引。
#
# 未設定時的行為（scripts/vercel-build.sh 會自動推導）：
#   production 部署 → https://$VERCEL_PROJECT_PRODUCTION_URL
#   preview 部署    → https://$VERCEL_URL
#   本機            → http://localhost:5173
#   （刻意不再 fallback 到寫死的測試網域）
#
# 格式：含 protocol、不含結尾斜線。
VITE_SITE_URL=http://localhost:5173
