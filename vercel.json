{
  "buildCommand": "cd frontend && npm install && npm run build:client && cd ../backend && npm install",
  "outputDirectory": "frontend/dist/client",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/server"
    }
  ]
}
