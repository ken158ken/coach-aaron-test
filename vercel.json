{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && mkdir -p .vercel_build_output && cp -r frontend/dist/client/* .vercel_build_output/ && cp -r frontend/dist/server .vercel_build_output/server && cd backend && npm install",
  "outputDirectory": ".vercel_build_output",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/server"
    },
    {
      "source": "/:path*",
      "destination": "/api/ssr"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
